import "./index.scss";
import { Sync } from "@mui/icons-material";

export default function Logo() {
    return (
        <div className="logo">
            Randomizer Tools <Sync />
        </div>
    );
}